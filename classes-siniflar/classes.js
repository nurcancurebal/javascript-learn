// JavaScript, nesne tabanlı bir programlama dilidir. JavScript'teki her şey, özellikleri ve yöntemleriyle bir nesnedir. Bir nesne oluşturmak için sınıf oluşturuyoruz. Bir Sınıf, bir nesne oluşturucu veya nesneler oluşturmak için bir "plan" gibidir Bir nesne oluşturmak için bir sınıf başlatıyoruz. Sınıf, nesnenin niteliklerini ve davranışını tanımlarken, nesne ise sınıfı temsil eder.

// Bir sınıf oluşturduğumuzda, ondan istediğimiz zaman nesne oluşturabiliriz. Bir sınıftan nesne oluşturmaya sınıf başlatma denir.

// Nesne bölümünde, bir nesne değişmezinin nasıl oluşturulacağını gördük. Nesne değişmezi bir tekildir(singleton). Benzer bir nesne elde etmek istiyorsak, onu yazmamız gerekiyor. Ancak sınıf, birçok nesne oluşturmaya izin verir. Bu, kod miktarını ve kod tekrarını azaltmaya yardımcı olur.

// Sınıf Tanımlama

// JavaScript'te bir sınıf tanımlamak için class anahtar kelimesine, CamelCase'deki bir sınıfın adına ve blok koduna (iki küme parantezine) ihtiyacımız var. Kişi adında bir sınıf oluşturalım.


class ClassName {

    // kod buraya gelecek
};



class Person {

    // kod buraya gelecek
};

// Bir Person sınıfı oluşturduk ama içinde hiçbir şey yok.